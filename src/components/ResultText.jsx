const ResultText = ({ children }) => {
  return (
    <strong className="font-semibold text-lg mt-2 block text-center text-gray-700">
      {children}
    </strong>
  );
};

export default ResultText;
